import { createAction } from "@ngrx/store";

export const viewCreate = createAction('change the component list to create');
export const viewList = createAction('change the component create to list');
